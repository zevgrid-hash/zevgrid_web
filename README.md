# ZevGrid Web

ZevGrid is a React marketplace portal for commercial EV rentals. The app has public catalog pages, business signup and requirement capture, dealer inventory workflows, and admin moderation dashboards.

## Run Frontend

```bash
npm start
```

Frontend dev server: `http://localhost:3000`
